{% extends 'default.html.twig' %}
{% block Form %}
    <div class="container" style="color: white">
        <h1> Recept Toevoegen</h1>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-dark"style="margin-top: 10px">Toevoegen</button>
        {{ form_end(form) }}
    </div>
{% endblock %}